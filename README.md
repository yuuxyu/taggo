# taggo

Markdown を使った、タグ管理のローカル Wiki デスクトップアプリです（Windows 専用）。
好きなエディタで書いた Markdown のフォルダを開くと、タグとリンクでたどれる Wiki として読めます。

## 概要

- **Markdown のノートだけを扱うローカル Wiki**
  - フォルダ配下の Markdown を、1 枚ずつのカードとしてフラットに並べて表示します。
  - 本文で画像や YouTube 動画を使っているノートは、最初の画像または動画のサムネイルをカードに表示します。
  - タグとキーワードで絞り込み、ノート同士のリンクと関連ページでたどれます。
  - アプリ内ではフォルダの階層を見せません。
- **タグはノート自身が持つ**
  - タグは Markdown の Front Matter（YAML）へ書き込みます。
  - taggo は独自のデータベースを持ちません。ノート自身のタグを唯一の正の情報として扱います。
  - taggo を使わなくなってもタグはノートと一緒に残り、他のツールからも読めます。

## ノート

- Markdown 先頭の YAML Front Matter に `tags:` で複数タグ指定可能
- GFM・コードハイライト・mermaid サポート
- Markdown 同士のリンクをたどる移動
- リンクする側・される側の関連ページ表示
- 同じタグを持つ関連ページ表示
- タグそのものを説明するタグページ（`tag:` を宣言した Markdown）
- URL だけを書いた段落のリンクカード表示（YouTube・X 対応）
- 本文の埋め込み画像をクリックすると画像ビューアを開く。拡大縮小、全体表示、原寸表示
- md 拡張子に紐づいたエディタで開くボタン（保存した変更は自動で反映）

## あえてしないこと

- **ノートの本文は編集しない。** 本文の編集や画像の加工はせず、既存のファイルで触るのは Front Matter のタグだけです。本文は好きなエディタで書き、taggo はその変更を拾います。まだ無いノートへのリンクをクリックしたときだけ、見出しだけのファイルを新しく作ってエディタへ渡します。
- **`[[WikiLink]]` を使わない。** ノート同士のリンクは普通の Markdown のリンクで書き、他のツールでもそのまま辿れるようにします。

## 使い方

[docs/使い方.md](docs/使い方.md) にまとめています。

---

## 機能一覧

### ノート自身のタグ

Front Matter（YAML）形式の `tags:` キーで複数割り当て可能です。

```markdown
---
tags:
  - golang
  - 開発メモ
---

# ここから本文
```

- 読み取りでは `keywords:` も `tags:` の別名として受け付けます。書き込むときは `tags:` にまとめます。

### タグページ

Front Matter に `tag:` を書いた Markdown は、そのタグを説明する「タグページ（タグそのものを表すページ）」になります。

```markdown
---
tag: golang
tags:
  - プログラミング
---

# Go

Go 言語に関するメモの入口。書き方の方針は [コーディング規約](./go-style.md) を参照。
```

- `tag:` に書けるタグは 1 つだけです。リストで複数書いたものはタグページとみなしません。
- 同じタグを複数のファイルが宣言していると、警告を出したうえで全部を表示します。どれか 1 つに絞ってください。
- `#タグ` で検索すると、そのタグのタグページがグリッドの上に見出しとして出ます。一覧のカードには混ざりません。

### 関連ページ

本文の右に関連ページをリスト表示します。

- このページからリンク
- このページへのリンク
- まだ無いノート（まだ書いていないノートへのリンク）
- 同じタグのノート

### まだ無いノート

まだ書いていないノートへのリンクは「まだ無いノート」として点線で出て、本文中では赤みがかった色になります。どちらもクリックすると、リンクが指す場所に Markdown ファイルを作って既定のエディタで開きます。

### 画像埋め込み

```markdown
![構成図](./images/architecture.png)
```

- 相対パスは Markdown ファイルの位置を基準に、`/` 始まりは開いているフォルダを基準に解決します。
- 表示できるのは、開いているフォルダ配下にある JPEG・PNG・GIF・WebP・AVIF・BMP・SVG です。

### Youtube や X のリンクカード

Markdown の本文に URL だけを 1 行で書くと、プレビューではリンク先のタイトル・説明・画像を載せたカードになります。

```markdown
https://www.youtube.com/watch?v=xxxxxxxxxxx
```

- 文中のリンクや、`[名前](https://…)` のように表示名を付けたリンクはカードにしません。
- 一般のページは OGP を、YouTube と X（投稿の URL）はそれぞれの oEmbed を読みます。X の投稿は画像を出さず、本文だけを表示します。

### ユーザ設定

設定は `%APPDATA%\taggo\settings.json` に保存します。

- 起動時に開くフォルダ（開かない・前回開いたフォルダ・指定したフォルダ）
- 一覧の既定の並び順（ツールバーで切り替えた並び順は保存しません）
- 1 回の走査の上限件数（デフォルト 20,000 件。1,000〜10 万件。次にフォルダを開いたときと、続きを読み込むときから反映）
- テーマ（OS に合わせる・ライト・ダーク）

### クラウド同期フォルダを開くとき

Windows の Cloud Files API を使う同期サービス（Dropbox や OneDrive）であれば、次のように振る舞ってダウンロードを起こしません。

- フォルダ走査そのものはファイルを開きません（ディレクトリの列挙だけで済ませます）。
- 中身がクラウド上にしか無いファイルは、ファイル属性（`RECALL_ON_DATA_ACCESS` など）から見分けて**一度も開きません**。名前・サイズ・更新日時だけで一覧に出し、「クラウド上のみ」と表示します。タグは読めないのでタグ検索にはかかりませんが、ファイル名では検索できます。
- そうしたファイルにはプレビューを要求しません。タグの書き込みも拒否します。
- 本文に埋め込んだ画像の中身がクラウド上にしか無いときは、その画像を表示しません。ノートを開いただけでダウンロードが始まらないようにするためです。
- 詳細プレビューの「ダウンロードして読み込む」を押したときだけ、そのファイルを読み込みます。
- 読み込んだあとで同期サービスが「オンラインのみ」へ戻したファイルも、開く直前に属性を確かめ直して見分けます。
