# 変更履歴

このプロジェクトの主な変更をこのファイルに記録します。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、バージョン番号は [セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

## [0.3.0] - 2026-09-26

一覧のカードとプレビューを見直し、検索からまだ無いタグのページを作れるようにしました。

### 追加

- 検索語と同じ名前のタグのページがまだ無いとき、検索結果の上に「`タグ.md` を作成する」ボタンを表示（押すと、まだ無いページの「md ファイルを作成する」と同じく、見出しだけのファイルを作ってエディタで開く）
- プレビューの下端に本文の文字数を表示（改行を除き、絵文字なども見た目の 1 文字として数える）

### 変更

- 一覧のカードをタイトルとタグだけにし、タグ欄を 2 行ぶんから 4 行ぶんに広げる。拡張子・ファイルサイズ・「未ダウンロード」の表示は外した（クラウド上にしか無いことは、カードの「クラウド上のみ」で分かる）
- 本文の `[[タグ]]` とノートへのリンクは下線なしで、既定のブラウザで開く外部リンクは下線と末尾の「↗」付きで表示し、ホバーで行き先の URL を出す

### 削除

- プレビュー下端の「前へ」「次へ」と、左右キーで一覧の並びどおりに隣のノートへ移る操作（リンクをたどった履歴の「←」「→」と `Alt` + 左右キーはそのまま使える）

## [0.2.0] - 2026-09-26

ノートのファイル名がそのままタグを表し、本文に `[[タグ]]` と書いてタグを付ける Wiki に作り変えました。taggo は既存のノートへ書き込まなくなりました。

### 移行するとき

- Front Matter の `tags:` はタグとして読まなくなりました。これまでのタグを残すには、エディタで本文に `[[タグ]]` と書き直してください。
- 開いたフォルダの直下にあるノートだけを読むようになりました。サブフォルダの中のノートは一覧に出ません。

### 追加

- ノートのファイル名（拡張子を除く）が、そのタグのページになる（`golang.md` は `golang` タグのページ）
- 本文の `[[タグ]]` でタグを付け、プレビューではそのタグのページへのリンクとして表示
- 一覧の先頭へのピン留め（フォルダ直下の `.taggo.json` に保存し、外で書き換えても読み直す）
- 検索語がタグの名前と一致するとき、そのタグのページを結果の先頭に表示
- まだ無いページを、ファイルを作らずに開いて関連ページを表示し、「md ファイルを作成する」ボタンから作る
- カードのタグ欄に収まらないタグを「+N」で示し、タグ欄に乗ると全部を表示

### 変更

- 関連ページを本文の右の欄から本文の下のグリッドにし、「リンク元」「タグごと（タグのページと、そのタグを持つノート）」「リンク先」のグループで並べる。前のグループに出たノートは後のグループに出さない
- 検索を、空白で区切った語をすべて含むノートを探す単純な全文検索にする
- まだ無いノートへのリンクをクリックしても、すぐにはファイルを作らない
- 先頭の `---` で囲んだブロック（ほかのツールの Front Matter）は、中身を読まずにプレビューと抜粋から外すだけにする

### 削除

- Front Matter の `tags:`・`keywords:`・`title:` の読み取り
- Front Matter の `tag:` で宣言するタグページ
- プレビューでのタグ編集と、複数のノートを選んでのタグの一括編集
- 検索の `#タグ`・`-#タグ`・`OR` の記法と、`#` を入力したときのタグ候補

### 修正

- ノートを保存しても、更新日時の順の一覧で先頭に移らないことがある
- 検索中にノートを書き換えても、検索結果に反映されない

## [0.1.0] - 2026-09-22

最初のリリースです。Markdown を使った、タグ管理のローカル Wiki デスクトップアプリ（Windows 専用）です。

### 追加

- フォルダ配下の Markdown を、1 枚ずつのカードとしてフラットに並べて表示
- 本文で最初に使っている画像か YouTube 動画のサムネイルをカードに表示
- タグとキーワードでの絞り込み
- Front Matter（YAML）の `tags:` によるタグの読み書き（`keywords:` も読み取り時に `tags:` の別名として扱う）
- `tag:` を宣言した Markdown を、そのタグを説明するタグページとして表示
- Markdown プレビュー（GFM・コードハイライト・mermaid）
- ノート同士のリンクをたどる移動と、移動履歴
- 関連ページ（このページからリンク・このページへのリンク・まだ無いノート・同じタグのノート）
- まだ無いノートへのリンクから、ノートを作って既定のエディタで開く
- 本文の埋め込み画像の表示と画像ビューア（拡大縮小・全体表示・原寸表示）
- URL だけを書いた段落のリンクカード表示（一般ページは OGP、YouTube と X は oEmbed）
- 外部リンクを既定のブラウザで開く
- md 拡張子に紐づいたエディタで開くボタンと、保存した変更の自動反映
- 2 万件を超えるフォルダの続きの読み込み
- クラウド同期フォルダ（Dropbox・OneDrive など）で、中身がクラウド上にしか無いファイルを開かずに扱う
- ユーザ設定（起動時のフォルダ・既定の並び順・走査の上限件数・テーマ）
- `--folder` オプションで開くフォルダを指定して起動

[0.3.0]: https://github.com/yuuxyu/taggo/releases/tag/v0.3.0
[0.2.0]: https://github.com/yuuxyu/taggo/releases/tag/v0.2.0
[0.1.0]: https://github.com/yuuxyu/taggo/releases/tag/v0.1.0
